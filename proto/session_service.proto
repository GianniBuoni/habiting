syntax = "proto3";
package habiting;

import "session_start.proto";
import "session_stop.proto";

service SessionService {
  rpc SessionStart (SessionStartRequest) returns (SessionStartResponse);
  rpc SessionStop (SessionStopRequest) returns (SessionStopResponse);
}
